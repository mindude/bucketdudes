'use strict';

/**
 * @ngdoc function
 * @name bucketdudesApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the bucketdudesApp
 */
angular.module('bucketdudesApp')
  .controller('MainCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
